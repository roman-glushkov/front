# Разбор Toolbar.tsx

Этот компонент представляет панель инструментов редактора презентаций. Он содержит набор кнопок для выполнения различных действий: добавление, удаление, перемещение слайдов, а также редактирование элементов и изменение фона.

Команды и конструкции

import React from 'react' — импорт React для использования JSX.
import './Toolbar.css' — подключение CSS-стилей панели инструментов.

interface Props { ... } — описание пропсов:
onAction — функция, которая вызывается при нажатии на любую кнопку и получает строку с названием действия.

export default function Toolbar({ onAction }: Props) — определение функционального компонента с деструктуризацией пропсов.

return (...) — JSX-разметка панели инструментов.

<div className="header toolbar-split"> — контейнер панели инструментов.  
<div className="toolbar-group"> — группа кнопок, объединённых по типу действий.

Первая группа — управление слайдами:
<button onClick={() => onAction('Добавить слайд')}>➕ Слайд</button> — добавляет новый слайд.
<button onClick={() => onAction('Удалить слайд')}>🗑️ Удалить</button> — удаляет выбранный слайд.
<button onClick={() => onAction('Переместить слайд')}>🔀 Переместить</button> — перемещает слайд.

Вторая группа — работа с текстом:
<button onClick={() => onAction('Добавить текст')}>📝 Текст</button> — добавляет текстовый элемент.
<button onClick={() => onAction('Изменить текст')}>✏️ Изменить</button> — меняет содержимое текста.
<button onClick={() => onAction('Изменить размер текста')}>🔠 Размер</button> — изменяет размер шрифта.
<button onClick={() => onAction('Изменить шрифт')}>🎨 Шрифт</button> — меняет стиль шрифта.

Третья группа — работа с элементами:
<button onClick={() => onAction('Добавить изображение')}>🖼️ Картинка</button> — добавляет изображение.
<button onClick={() => onAction('Удалить элемент')}>❌ Удалить элемент</button> — удаляет выбранный элемент.
<button onClick={() => onAction('Изменить позицию элемента')}>↔️ Позиция</button> — изменяет положение элемента.
<button onClick={() => onAction('Изменить размер элемента')}>📏 Размер</button> — изменяет размеры элемента.

Четвёртая группа — оформление:
<button onClick={() => onAction('Изменить фон')}>🎨 Фон</button> — изменяет фон слайда.

Каждая кнопка при нажатии вызывает функцию onAction с соответствующим названием действия, чтобы обработчик из хука useEditor выполнил нужное изменение. Компонент не имеет своего состояния.
