1.4

1. Созданы типы Position и Size, BaseElement в файле types of presentation.ts

2. Во всех функциях, где создаются новые объекты, используются копии:
   { ...newSlide } при добавлении слайда
   { ...textElement } и { ...imageElement } при добавлении элементов
   { ...newPosition } и { ...newSize } при изменении позиции и размера
   { ...newBackground } при изменении фона
