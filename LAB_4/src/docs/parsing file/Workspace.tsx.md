# Разбор Workspace.tsx

Этот компонент отображает рабочую область, где показан текущий слайд и его элементы. Здесь пользователь может видеть текстовые и графические объекты и выбирать их для редактирования.

interface Props { ... } — описание пропсов компонента:
slide — объект текущего слайда (может быть undefined, если слайд не выбран).
selElId — id выбранного элемента.
onElementClick — обработчик клика по элементу слайда.

export default function Workspace({ slide, selElId, onElementClick }: Props) — определение функционального компонента с деструктуризацией пропсов.

return (...) — JSX-разметка рабочей области.

<div className="workspace-panel"> — контейнер компонента.  
<h3>Рабочая область</h3> — заголовок панели.  
<div className="workspace"> — область, в которой отображается содержимое выбранного слайда.

slide ? ( ... ) : ( ... ) — проверка: если слайд выбран, показываем его содержимое, иначе — сообщение «Выберите слайд».

<div className="workspace-content" style={{ backgroundColor: ... }}> — область текущего слайда.  
backgroundColor задается из объекта slide.background: если тип — «color», используется цвет, иначе — белый.

slide.elements.map((el) => { ... }) — перебор всех элементов на слайде.
const textEl = el as TextElement — приведение типа элемента к текстовому, если это текст.
const backgroundColor = el.type === 'text' ? textEl.color : '#e0e0e0' — цвет фона для элемента: текст берёт свой цвет, а другие элементы — серый.

<div key={el.id} ...> — блок для отдельного элемента слайда.  
key={el.id} — уникальный ключ.  
onClick={() => onElementClick(el.id)} — при клике вызывается обработчик выбора элемента.  
className={`element ${selElId === el.id ? 'selected' : ''}`} — добавляется класс выделения, если элемент выбран.  
style={{ left, top, width, height, backgroundColor, font }} — inline-стили элемента:
left, top — позиция элемента,  
width, height — размер,  
backgroundColor — цвет,  
font — шрифт и размер текста, если элемент текстовый.  
Внутри блока отображается содержимое:
если элемент текстовый — textEl.content,  
если нет — иконка 🖼️ (изображение).

Если слайд не выбран, выводится <p>Выберите слайд</p>.
